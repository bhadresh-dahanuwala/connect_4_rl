Create the following test cases to check MCTS works correctly:

1. 3-in-a-row straight: Place on the right end to win
2. 3-in-a-row straight: Place on the left end to win
3. 3-in-a-row diagonal: Place on the top end to win
4. 3-in-a-row diagonal: Place on the bottom end to win
5. 3-in-a-row anti-diagonal: Place on the top end to win
6. 3-in-a-row anti-diagonal: Place on the bottom end to win

The above were to win. Add similar test cases to check whether the block also works.

We also need to ensure the value heads work correctly.


Here is the logic. Please ensure it is implemented as per this logic.

```
for iter in range(1, 5001):
    if iter == 1:
        M0 = Random Weights
        Self Play = M0 vs M0
        M1 = Train(M0)
        Eval = M0 vs M1
        If M0 win rate > M1 win rate then
            Best = M0
        Else
            Best = M1

    else if iter == 2:
        Self Play = Best vs Best
        M2 = Train(Best)
        Eval = Best vs M2
        if M2 win rate >= 62% then
            Best = M2
    else:
        prev_iter = iter - 1
        Self Play = Best vs M{prev_iter}
        M{iter} = Train(M{prev_iter})
        Eval = Best vs M{iter}
        if M{iter} win rate >= 62% then
            Best = M{iter}
```
