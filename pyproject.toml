[project]
name = "connect-4-rl"
version = "0.1.0"
description = ""
authors = [
    {name = "BD"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "torch (>=2.0.0)",
    "gymnasium (>=1.0.0)",
    "pygame (>=2.6.1,<3.0.0)"
]

[tool.poetry]

[tool.poetry.group.dev.dependencies]
pytest = "^9.0.2"
pylint = "^4.0.4"
mypy = "^1.19.1"
flake8 = "^7.3.0"
pyinstaller = {version = ">=6.17.0", python = ">=3.13,<3.15"}

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
